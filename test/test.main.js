var assert = require('assert')
,socket_party = require('./../public/javascripts/main').socket_party;

suite('socket_party', function() {
	test('send message should append message to window', function() {
		assert.equal($(''));
	});
});
